"""Mappers for Domain <-> Records conversion.

TODO: Implement mappers for:
- ProblemMapper: Problem <-> ProblemRecord + related records
- TemplateMapper: ProblemTemplate <-> TemplateRecord + TestCaseRecord + CanonicalSolutionRecord
- UserMapper: User <-> UserRecord
- SettingsMapper: Settings <-> SettingsRecord
- DraftMapper: Draft <-> DraftRecord (requires Problem, Template, User)
- SubmissionMapper: Submission <-> SubmissionRecord (requires Problem, Template, User)
"""
